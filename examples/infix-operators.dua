no_mangle int printf(str message, ...);

void print(int num) { printf("%d", num); }


// Case Infix +
// Outputs "5"

class X
{
    int num = 3;

    int infix + (Y* y) {
        return self.num + y->num;
    }
}

class Y
{
    int num = 2;
}

int main()
{
    X x;
    Y y;
    print(&x + &y);
}

