// CASE Basic if expression
// Returns 5

int main()
{
    int i = 3;
    return if (i < 2) { 4 } else { 5 };
}


// CASE If-else if-else
// Returns 10

int main()
{
    int k = 5;
    return if (k > 10) { 4 }
           else if (k < 2) { 5 }
           else { 10 };
}
